rules_version = '2';

// All waitlist data is written and read only by the Next.js server through the
// Firebase Admin SDK, which bypasses these rules. Browsers get no direct access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
